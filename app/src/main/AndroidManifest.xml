<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openclassrooms.realestatemanager">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/RealEstateTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".controler.LoanSimulatorActivity"
            android:label="@string/loan_simulator_label"
            android:parentActivityName=".controler.MainActivity"/>

        <activity
            android:name=".controler.DetailsActivity"
            android:label="@string/details_label"
            android:parentActivityName=".controler.MainActivity" />

        <activity android:name=".controler.DetailsFromMapActivity"
            android:label="@string/details_label"
            android:parentActivityName=".controler.MainActivity" />

        <activity
            android:name=".controler.MapsEstateActivity"
            android:label="@string/map_label"
            android:parentActivityName=".controler.MainActivity" />
        <activity
            android:name=".controler.ModifyEstateActivity"
            android:parentActivityName=".controler.MainActivity" />
        <activity
            android:name=".controler.CreateEstateActivity"
            android:parentActivityName=".controler.MainActivity" />

        <provider
            android:name=".provider.EstateContentProvider"
            android:authorities="com.openclassrooms.realestatemanager.provider"
            android:exported="true" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.openclassrooms.realestatemanager.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".controler.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>